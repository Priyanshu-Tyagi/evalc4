import { Actions } from "./actionCreators";

const reducer = (state,action) => {
    switch (action.type){
        case Actions.GET_PRODUCTS_REQUEST:
            return {
                
            }
    }
}